// All files that need SDL should include this rather than including SDL directly
// (Because I'm never quite sure where package managers are going to put things)

#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

